import React from 'react'

const MessageTimestamp = () => {
  return (
    <div>
       this message timestamp component is for showing the time when the message was sent or received. It can be used in a chat application to display the timestamp of each message in the chat conversation. The component can be styled to match the overall design of the chat interface and can be placed next to each message bubble to provide context for when the message was sent or received.
    </div>
  )
}

export default MessageTimestamp
